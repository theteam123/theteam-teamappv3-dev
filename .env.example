# ==========================================
# Main ERPNext Instance Configuration
# ==========================================

# The base URL for the main ERPNext instance
# Example: https://erp.theteam.net.au
VITE_ERPNEXT_API_URL=

# API Key for the main ERPNext instance
# This is used for authentication with the main ERPNext API
# You can find this in ERPNext > Setup > Integrations > API Access
VITE_ERPNEXT_API_KEY=

# API Secret for the main ERPNext instance
# This is used for authentication with the main ERPNext API
# You can find this in ERPNext > Setup > Integrations > API Access
VITE_ERPNEXT_API_SECRET=

# ==========================================
# Taktec ERPNext Instance Configuration
# ==========================================

# The base URL for the Taktec ERPNext instance
# Example: http://taktec.theteam.net.au
VITE_TAKTEC_ERPNEXT_API_URL=

# API Key for the Taktec ERPNext instance
# This is used for authentication with the Taktec ERPNext API
# You can find this in ERPNext > Setup > Integrations > API Access
# VITE_TAKTEC_ERPNEXT_API_KEY=

# API Secret for the Taktec ERPNext instance
# This is used for authentication with the Taktec ERPNext API
# You can find this in ERPNext > Setup > Integrations > API Access
VITE_TAKTEC_ERPNEXT_API_SECRET=

# ==========================================
# Environment Notes
# ==========================================
# 1. Copy this file to .env.local for local development
# 2. Copy this file to .env.production for production
# 3. Never commit .env.local or .env.production to version control
# 4. Make sure all values are properly set before deploying
# 5. For local development, you can use the example values
# 6. For production, use the actual API keys and secrets