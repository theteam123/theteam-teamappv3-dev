# ==========================================
# Main ERPNext Instance Configuration
# ==========================================

# OAuth Configuration
VITE_OAUTH_CLIENT_ID=your_client_id_here
VITE_OAUTH_CLIENT_SECRET=your_client_secret_here
VITE_OAUTH_REDIRECT_URI=http://localhost:5173/auth/callback

# ERPNext API Configuration
VITE_ERPNEXT_API_URL=https://erp.theteam.net.au

# Note: Replace the placeholder values with your actual credentials
# VITE_OAUTH_CLIENT_ID: Your OAuth client ID from ERPNext
# VITE_OAUTH_CLIENT_SECRET: Your OAuth client secret from ERPNext
# VITE_OAUTH_REDIRECT_URI: The callback URL where users will be redirected after authentication
# VITE_ERPNEXT_API_URL: Your ERPNext instance URL

# ==========================================
# Environment Notes
# ==========================================
# 1. Copy this file to .env.local for local development
# 2. Copy this file to .env.production for production
# 3. Never commit .env.local or .env.production to version control
# 4. Make sure all values are properly set before deploying
# 5. For local development, you can use the example values
# 6. For production, use the actual API keys and secrets